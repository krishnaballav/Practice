sc-> o(1)
tc -> O(1)
https://practice.geeksforgeeks.org/problems/delete-without-head-pointer/1

void deleteNode(Node *node)
{
   // Your code here
   
      
     Node *temp=node->next;
     node->data=temp->data;
     node->next=temp->next;
   
   
   
}
